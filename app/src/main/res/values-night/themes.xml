<resources>
    <!-- Base application theme. -->
    <style name="Theme.GreenStash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSurface">@color/dark_gray</item>
        <item name="colorOnPrimaryContainer">@color/white</item>
        <item name="backgroundColor">@color/black</item>
        <item name="android:windowBackground">?attr/backgroundColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <item name="iconPreferenceColor">?attr/colorOnPrimaryContainer</item>
        <item name="cardButtonIconColor">?attr/colorOnPrimaryContainer</item>
        <item name="cardBackgroundColor">?attr/colorSurface</item>
        <item name="transactionRowBackground">@color/color_primary_dark</item>
        <item name="fabBackgroundColor">?attr/colorPrimary</item>
        <item name="fabIconColor">?attr/colorOnPrimary</item>
        <item name="switchStyle">@style/Widget.App.Switch</item>
        <item name="materialAlertDialogTheme">@style/AlertDialogTheme</item>
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <item name="autoCompleteTextViewStyle">@style/SearchAutoCompleteTextView</item>
    </style>

    <!-- Toolbar options menu color. -->
    <style name="ToolbarPopupStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">?attr/colorOnPrimary</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Switch color. -->
    <style name="Widget.App.Switch" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="">
        <item name="colorOnSurface">@color/dark_green</item>
        <item name="colorSecondary">@color/color_secondary</item>
    </style>

    <!-- alert dialog color. -->
    <style name="AlertDialogTheme" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="buttonBarNeutralButtonStyle">@style/NeutralButtonStyle</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <style name="NeutralButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <!-- SearchView text color -->
    <style name="SearchAutoCompleteTextView" parent="Widget.AppCompat.Light.AutoCompleteTextView">
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="android:textColorHint">?attr/colorOnPrimary</item>
    </style>

</resources>